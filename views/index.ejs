<!DOCTYPE html>
<html>
<head>
    <title>MovieWeb</title>
    <style>
        body {
            background-image: url('Net.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center center;
            font-family: 'Arial', sans-serif;
            background-color: #F7E6C4;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            border-radius: 10px;
            padding: 20px;
            text-align: center;
        }

        h1 {
            font-size: 100px;
            color: white;
            margin-bottom: 20px;
            padding: 10px;
            margin-top: 10px;
        }

        .title-container {
            display: inline-block;
            backdrop-filter: blur(2px); /* Adjust the blur intensity as needed */
            background-color: rgba(255, 255, 255, 0.01); /* Adjust the opacity as needed */
            border-radius: 10px;
            padding: 20px;
            margin-top: 10px;
        }

        form {
            text-align: center;
            margin-top: 20px;
            margin-bottom: 200px;
        }

        input[type="text"] {
            padding: 12px;
            width: 300px;
            border-radius: 4px;
            font-size: 18px;
            margin-right: 10px;
            outline: none;
            background-color: rgba(255, 255, 255, 0.1);
        }

        button[type="submit"] {
            background-color: #12486B;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 12px 20px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<div class="container"> <!-- Contenedor alrededor del título y el buscador -->
    <div class="title-container">
        <h1>🎥 MovieWeb🎬</h1>
    </div>
    <!-- Input del buscador -->
    <form action="/buscar" method="GET">
        <input type="text" name="q" placeholder="Buscar..." />
        <button type="submit">Buscar</button>
    </form>
</div>
</body>
</html>

